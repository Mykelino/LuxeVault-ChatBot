import { GoogleGenerativeAI } from '@google/generative-ai'; import 'dotenv/config'; const genAI = new GoogleGenerativeAI(process.env.GEMINI_API_KEY || ''); async function test() { try { const model = genAI.getGenerativeModel({ model: 'gemini-3-flash' }); const result = await model.generateContent('hi'); console.log('Gemini 3 Flash works!'); } catch (e) { console.error('Gemini 3 Flash failed:', e.message); } try { const model = genAI.getGenerativeModel({ model: 'gemini-3.1-flash' }); const result = await model.generateContent('hi'); console.log('Gemini 3.1 Flash works!'); } catch (e) { console.error('Gemini 3.1 Flash failed:', e.message); } } test();
