import { GoogleGenerativeAI } from '@google/generative-ai'; import 'dotenv/config'; async function list() { try { const genAI = new GoogleGenerativeAI(process.env.GEMINI_API_KEY || ''); // Use a model that usually exists to get the client initialized const model = genAI.getGenerativeModel({ model: 'gemini-1.5-flash' }); // listModels is only available on the Generative AI Client in advanced scenarios? No, it should be available. } catch (e) { console.error(e.message); } } list();
